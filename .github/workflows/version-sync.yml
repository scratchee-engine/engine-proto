name: Sync package.json version with git tag

on:
  push:
    tags:
      - 'v*'

jobs:
  sync-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Extract version from tag
        id: version
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          VERSION=${TAG_NAME#v}
          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted tag: $TAG_NAME, version: $VERSION"

      - name: Update package.json version
        run: |
          VERSION=${{ steps.version.outputs.version }}
          jq --arg v "$VERSION" '.version = $v' package.json > package.json.tmp
          mv package.json.tmp package.json
          echo "Updated package.json to version $VERSION"

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet package.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No changes needed - package.json already at correct version"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Changes detected in package.json"
          fi

      - name: Commit and push version bump
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json
          git commit -m "chore: bump package.json version to ${{ steps.version.outputs.version }}"
          git push origin HEAD:main

      - name: Move tag to updated commit
        if: steps.changes.outputs.changed == 'true'
        run: |
          TAG_NAME="${{ steps.version.outputs.tag_name }}"
          git tag -f "$TAG_NAME"
          git push --force-with-lease origin "$TAG_NAME"
          echo "Moved tag $TAG_NAME to commit with updated package.json"
